<div id="nav">
  You don't have access to this tool. Anyone who has access can grant you access. Try asking one of the following people:

  <ul>
    <% User.order(:last_name).each do |user| %>
      <li><%= user.name %> - <%= mail_to(user.username) %></li>
    <% end %>
  </ul>
</div>